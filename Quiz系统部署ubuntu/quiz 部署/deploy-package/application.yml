spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/quiz?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: testme
    password: 12345

server:
  port: 8080
  address: 127.0.0.1

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.quiz.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: quiz-secret-key-for-jwt-token-generation-and-validation-must-be-long-enough-for-hs512
  expiration: 3600000
